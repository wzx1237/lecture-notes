name: COMP3211
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  # --- Core dependencies (Aligned with Colab Defaults) ---
  - python=3.12.*
  - numpy>=2.0.0
  - pandas>=2.2.2
  - scipy>=1.15.0
  - scikit-learn>=1.6.0
  - matplotlib>=3.10.0
  
  # --- PyTorch (Standard Colab GPU acceleration) ---
  - pytorch>=2.6.0
  - torchvision>=0.21.0
  - torchaudio>=2.6.0
  - pytorch-cuda=12.1
  
  # --- Common Colab Pre-installed Utilities ---
  - ipykernel
  - notebook
  - tqdm
  - pillow>=11.0.0
  
  # --- PIP installed dependencies ---
  - pip
  - pip:
      # Core NLP and Vision models
      - transformers>=4.53.2
      - torchmetrics==1.3.1
      - einops==0.8.0
      - timm>=1.0.17
      - sentence-transformers>=5.1.0
      
      # Visualization and Tracking
      - tensorboard>=2.19.0
      - keybert
      
      # Additional Colab-standard libraries
      - accelerate>=1.9.0
      - datasets>=4.0.0
      - huggingface-hub>=0.33.4

# Create
# conda env create -f environment.yml
# Remove
# conda env remove -n <ENV name>
